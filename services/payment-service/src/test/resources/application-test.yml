stripe:
  secret:
    key: ${STRIPE_SECRET_KEY:sk_test_dummy_key_for_testing}
  publishable:
    key: ${STRIPE_PUBLISHABLE_KEY:pk_test_dummy_key_for_testing}
  webhook:
    secret: ${STRIPE_WEBHOOK_SECRET:whsec_dummy_secret_for_testing}
